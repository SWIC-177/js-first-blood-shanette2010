
// declaring variables 

const name = "Shanette";
const age = 31; 

if (age >= 18) {     
        return `Welcome, {name}!`;

        return `You are not old enough to enter, {name}.`;

        const adultMessage = verifyAdulthood(shanette, 31);

console.log(adultMessage);
        }
